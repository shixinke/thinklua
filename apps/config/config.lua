local config = {
    debug = true,
    database = {

    },
    routes = {
        default_controller = 'index',
        rules= {

        }
    },
    pages = {
        not_found = '',
        server_error = ''
    }

}
return config;